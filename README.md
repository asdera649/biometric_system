# Интеллектуальная система биометрической аутентификации

ВКР: «Интеллектуальная система авторизации пользователей сайта по лицу с интегрированной защитой от подделок»

## Стек технологий

| Слой | Технология |
|------|-----------|
| Backend | Django 4.2, Python 3.10+ |
| Детекция лица | MTCNN (facenet-pytorch) |
| Эмбеддинги | InceptionResNetV1 предобученная на VGGFace2 (512d) |
| Метрика схожести | Косинусное расстояние |
| Качество кадра | Лапласиан + яркость + контраст (OpenCV) |
| Живость | Заглушка — место для интеграции (LSTM/3D CNN) |
| Frontend | Bootstrap 5, Chart.js, Web Camera API |
| БД | SQLite (dev) / PostgreSQL (prod) |

## Быстрый старт

```bash
# 1. Установить зависимости
pip install -r requirements.txt

# 2. Применить миграции
python manage.py migrate

# 3. Создать администратора
python manage.py shell < create_superuser.py

# 4. (Опционально) Загрузить демо-данные
python manage.py shell < generate_demo_data.py

# 5. Запустить сервер
python manage.py runserver
```

## Учётные данные (по умолчанию)

| Роль | Логин | Пароль |
|------|-------|--------|
| Администратор / Оператор | `admin` | `admin123` |
| Тестовый пользователь | `testuser` | `test123` |

## Страницы

| URL | Описание |
|-----|----------|
| `/` | Главная страница |
| `/accounts/register/` | Регистрация (шаг 1 — личные данные) |
| `/accounts/register/biometric/` | Регистрация (шаг 2 — съёмка лица) |
| `/accounts/login/` | Вход (ввод логина) |
| `/accounts/capture/` | Захват лица для аутентификации |
| `/accounts/profile/` | Личный кабинет пользователя |
| `/operator/` | Дашборд оператора |
| `/operator/logs/auth/` | Журнал аутентификации |
| `/operator/logs/system/` | Системный журнал |
| `/operator/users/` | Управление пользователями |
| `/operator/reports/` | Отчёты и метрики нейросети |
| `/operator/settings/` | Настройки системы |

## Архитектура нейросети

### Пайплайн аутентификации:
```
Камера → MTCNN (детекция + выравнивание) → InceptionResNetV1 (512d эмбеддинг)
→ Косинусное сходство с эталоном → Порог → Решение (доступ / отказ)
→ [Место для Liveness Detection]
```

### Метрики качества (Отчёты → `/operator/reports/`):
1. **Уверенность распознавания** — косинусная схожесть с эталонным шаблоном (0–100%)
2. **Качество изображения** — комплексная оценка (резкость, яркость, контраст, разрешение)
3. **Детекция живости** — *(заглушка, место для интеграции LSTM/3D CNN)*

## Для ВКР: обоснование дообучения модели

Предобученная модель InceptionResNetV1 (VGGFace2) адаптирована:
- Пороги принятия решений откалиброваны под условия целевой системы
- Метрика качества настроена под характеристики входных камер
- Пространство эмбеддингов исследовано: FAR/FRR анализ через журнал попыток

## Место для интеграции Liveness Detection

```python
# apps/biometric/face_processor.py → process_authentication_image()
# Строка: liveness_score = None  # TODO: integrate liveness model

# Варианты интеграции:
# 1. Анализ текстуры: LBP/HOG-классификатор
# 2. Оптический поток: анализ движения между кадрами
# 3. Глубокое обучение: MobileNetV2 / 3D CNN / LSTM на последовательности кадров
```
